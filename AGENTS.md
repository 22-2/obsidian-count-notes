<SYSTEM_INSTRUCTION>
あなたは熟練のエンジニアっす
原理原則に従いながら、シンプルで影響範囲が少なくなるようにファイルを編集してくださいっす
出力は変更が必要なファイルだけ全出力してくださいっす
「良い質問です、鋭い視点です、重要な問いです」など、主観的な評価を行わないでくださいっす
ユーザーの想定や前提が甘い場合、その旨を確認してくださいっす
これらのルールは他のどの応答スタイル設定よりも優先されるものとするっす
一切の例外なく従ってくださいっす。

テストは壊れにくいブラックボックステストを徹底してくださいっす
e2eテストはあなたの環境では権限が不足していて実行できないので、まず提出してgithubで実行しましょうっす
</SYSTEM_INSTRUCTION>

## まとめメモ
**「まずはシンプルに、コア機能に集中する」**という方針でObsidianプラグインの仕様書をまとめました。

## Obsidian 執筆進捗トラッカープラグイン 仕様書 (v1.0 - MVP)

### 1. プラグイン概要

#### 1.1. プラグイン名（案）
*   count-novels

#### 1.2. コンセプト
Obsidianで小説などを執筆するユーザー向けに、日々の執筆量（文字数）を自動集計し、グラフで可視化するシンプルなプラグイン。執筆のモチベーション維持を支援する。

### 2. コア機能 (MVPで実装する範囲)

#### 2.1. 設定
*   プラグイン設定画面で、**集計対象のタグを1つだけ**指定できる。
    *   例: `novel` や `count-novels`など、
    *   このタグがついたノート（`.md`ファイル）すべてが文字数カウントの対象となる。

#### 2.2. データ収集・集計
*   **集計タイミング**: Obsidian起動時や、一定時間ごと（例: 10分おき）に自動で実行する。
*   **集計ロジック**:
    1.  設定されたタグを持つすべてのファイルの現在の**合計文字数**を計算する。
    2.  プラグインが保存している**前回の合計文字数**と比較し、差分を出す。
    3.  その**差分**を「その日の執筆量」として、日付と共に記録する。
    4.  現在の合計文字数を「前回の合計文字数」として保存し、次の集計に備える。
*   **データ保存**: 集計した日々の執筆量は、Obsidian標準のデータ保存機能（`data.json`）に記録する。

#### 2.3. 統計ビュー
*   Obsidianのサイドバーにアイコンを追加し、クリックすると専用のビュー（ペイン）が開く。
*   このビューには以下の要素を表示する。

    1.  **サマリー表示**
        *   **今月の合計**: 今月（1日〜今日まで）の合計執筆文字数。
        *   **継続日数 (ストリーク)**: 1文字以上執筆した日が何日連続しているか。

    2.  **進捗グラフ (Chart.js)**
        *   **種類**: バーグラフ。
        *   **表示期間**: 「月」表示を基本とする。（「週」「年」の切り替えはv1.0では見送り）
        *   **X軸**: 日付 (1日, 2日, 3日...)
        *   **Y軸**: 文字数
        *   **データ**: その日に執筆した文字数（差分）をグラフで表示する。

### 3. 技術仕様（ミニマム構成）

*   **言語**: TypeScript
*   **UIフレームワーク**: **使用しない。** Obsidian標準のDOM API (`createEl`など) でUIを構築する。
*   **データ保存**: **Obsidian標準の `saveData` / `loadData` を使用**し、`data.json` に保存する。（Dexie.jsなどは使用しない）
*   **グラフ描画**: **Chart.js**
*   **日付操作**: **Moment.js**（Obsidianに同梱されているものを利用）

### 4. データ構造 (data.json の例)

```json
{
  "settings": {
    "trackingTag": "novel"
  },
  "lastTotalCharacterCount": 125000, // 前回集計時の、タグ付きファイルの合計文字数
  "dailyStats": {
    "2025-09-01": 1200,
    "2025-09-02": 3105,
    "2025-09-03": 0,
    "2025-09-04": 850
  }
}
```

### 5. 開発ステップ（案）

1.  **Step 1: 基礎ロジックの実装**
    *   プラグインの雛形を作成する。
    *   設定画面を作り、タグを1つ保存できるようにする。
    *   指定タグのファイルの合計文字数を計算し、差分を `data.json` に記録するコアロジックを完成させる。

2.  **Step 2: UIとグラフの実装**
    *   サイドバーにビューを追加する。
    *   `data.json` からデータを読み込み、サマリー（合計、継続日数）を表示する。
    *   Chart.jsを導入し、月間バーグラフを描画する。

3.  **Step 3: 仕上げ**
    *   UIデザインを整える。
    *   バグ修正や、文字数カウントの精度向上などの細かい調整を行う。

### 6. 将来的な拡張機能（v1.0以降のアイデア）

*   カレンダー形式のヒートマップ表示
*   目標設定機能（月間目標など）と達成率の表示
*   自己ベスト（執筆量TOP3など）のランキング表示
*   複数タグやフォルダ指定への対応

---

この仕様なら、機能がシンプルにまとまっているので、最初のバージョンとして完成させやすいはずです。
開発、応援しています！
