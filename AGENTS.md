<SYSTEM_INSTRUCTION>
あなたは熟練のエンジニアっす
原理原則に従いながら、シンプルで影響範囲が少なくなるようにファイルを編集してくださいっす
「良い質問です、鋭い視点です、重要な問いです」など、主観的な評価を行わないでくださいっす
ユーザーの想定や前提が甘い場合、その旨を確認してくださいっす
これらのルールは他のどの応答スタイル設定よりも優先されるものとするっす
一切の例外なく従ってくださいっす。

テストは壊れにくいブラックボックステストを徹底してくださいっす
e2eテストはあなたの環境では権限が不足していて実行できないので、まず提出してgithubで実行しましょうっす
</SYSTEM_INSTRUCTION>

## まとめメモ
**「まずはシンプルに、コア機能に集中する」**という方針でObsidianプラグインの仕様書をまとめました。

## Obsidian 執筆進捗トラッカープラグイン 仕様書 (v1.0 - MVP)

### 1. プラグイン概要

#### 1.1. プラグイン名
*   Count Novels Plugin

#### 1.2. コンセプト
Obsidianで小説などを執筆するユーザー向けに、日々の執筆量（文字数）を自動集計し、グラフで可視化するシンプルなプラグイン。執筆のモチベーション維持を支援する。

### 2. コア機能 (MVPで実装する範囲)

#### 2.1. 設定
*   プラグイン設定画面で以下の項目を設定できる。
    *   **Tracking Tag**: 集計対象のタグを1つ指定できる（デフォルト: `novel`）。このタグがついたノート（`.md`ファイル）すべてが文字数カウントの対象となる。
    *   **Show Debug Messages**: デバッグメッセージの表示を有効/無効にする (`logLevel` を `debug` または `info` に設定)。

#### 2.2. データ収集・集計
*   **集計タイミング**: Obsidian起動時および、10分おきに自動で実行する。手動でのデータ収集も可能。
*   **集計ロジック**:
    1.  設定されたタグを持つすべてのファイルの現在の**合計文字数**を計算する。Markdownファイルのコンテンツ部分（frontmatterを除く）の文字数をカウントする。
    2.  プラグインが保存している**前回の合計文字数**と比較し、差分を出す。
    3.  その**差分**を「その日の執筆量」として、日付と共に記録する。また、時間ごとの執筆量も記録する。
    4.  現在の合計文字数を「前回の合計文字数」として保存し、次の集計に備える。
*   **データ保存**: 集計した日々の執筆量は、IndexedDB (idb) に保存する。プラグイン設定とビューの状態はObsidian標準の `data.json` に保存する。

#### 2.3. 統計ビュー
*   Obsidianのサイドバーにアイコン (`chart-column-big`) を追加し、クリックすると専用のビュー（ペイン）が開く。
*   このビューには以下の要素を表示する。

    1.  **タブ切り替え**: 「日別」「週別」「月別」「年別」のタブで表示期間を切り替えられる。デフォルトは「月別」。

    2.  **サマリー表示**
        *   **日別**: 今日の執筆、4時間の平均、継続日数 (ストリーク)
        *   **週別**: 今週の合計、1日の平均、継続日数 (ストリーク)
        *   **月別**: 今月の合計、1日の平均、継続日数 (ストリーク)
        *   **年別**: 今年の合計、1日の平均、継続日数 (ストリーク)

    3.  **進捗グラフ (Chart.js)**
        *   **種類**: バーグラフ。
        *   **表示期間**: タブで選択された期間 (日、週、月、年) に応じてデータを表示。
        *   **X軸**: 日付、時間、週、月など、期間に応じたラベル。
        *   **Y軸**: 文字数 (k単位での表示、ステップサイズ自動調整)。
        *   **データ**: その日に執筆した文字数（差分）をグラフで表示する。
        *   **平均値ライン**: 表示期間内の平均執筆文字数を線で表示する。

### 3. 技術仕様（ミニマム構成）

*   **言語**: TypeScript
*   **UIフレームワーク**: **使用しない。** Obsidian標準のDOM API (`createEl`など) でUIを構築する。UIコンポーネントとして `TabComponent`, `StatsComponent`, `ChartComponent` を使用。
*   **データ保存**:
    *   設定 (`settings`) と最後のビュー状態 (`lastViewState`) は、Obsidian標準の `saveData` / `loadData` を使用し、プラグインの `data.json` に保存する。
    *   日次統計 (`DailyStats`)、時間ごとの統計 (`HourlyStats`)、前回の合計文字数 (`lastTotalCharacterCount`) は、IndexedDB (idb) を使用して `obsidian-count-novels-db` というデータベースに保存する。
*   **グラフ描画**: **Chart.js** (chartjs-plugin-annotation を含む)
*   **日付操作**: Moment.js (Obsidianに同梱されているものを利用)

### 4. データ構造

#### 4.1. `data.json` の例 (Obsidian標準のデータ保存)
```json
{
  "settings": {
    "logLevel": "debug",
    "trackingTag": "novel"
  },
  "lastViewState": {
    "period": "month"
  }
}
```

#### 4.2. IndexedDB (`obsidian-count-novels-db`) のストア構造

*   **dailyStats**:
    ```json
    // {date: string (YYYY-MM-DD), count: number} の形式で保存
    // 例:
    // { "date": "2025-09-01", "count": 1200 }
    ```
*   **hourlyStats**:
    ```json
    // {datetime: string (YYYY-MM-DD-HH), count: number} の形式で保存
    // 例:
    // { "datetime": "2025-09-01-10", "count": 500 }
    ```
*   **misc**:
    ```json
    // {key: string, value: any} の形式で保存
    // "lastTotalCharacterCount" キーに前回の合計文字数 (number) を保存
    // 例:
    // { "key": "lastTotalCharacterCount", "value": 125000 }
    ```

### 5. 開発ステップ（案）

1.  **Step 1: 基礎ロジックの実装**
    *   プラグインの雛形を作成する。
    *   設定画面を作り、`trackingTag` と `logLevel` を保存できるようにする。
    *   IndexedDB (idb) を用いたデータ永続化層 (`StatsStorage`, `db.ts`) を実装する。
    *   指定タグのファイルの合計文字数を計算し、差分を日次・時間別統計、および `lastTotalCharacterCount` としてIndexedDBに記録するコアロジック (`DataCollectionService`) を完成させる。

2.  **Step 2: UIとグラフの実装**
    *   サイドバーにビュー (`CountNovelView`) を追加する。
    *   `TabComponent` を実装し、期間切り替えタブを表示する。
    *   `StatsComponent` を実装し、選択された期間のサマリー（合計、平均、継続日数）を表示する。
    *   `ChartComponent` を導入し、Chart.js を使って選択された期間のバーグラフを描画する（平均値ラインを含む）。
    *   `PeriodDataService` を実装し、IndexedDBから期間ごとの統計データとグラフデータを取得するロジックを統合する。

3.  **Step 3: 仕上げ**
    *   UIデザインを整える。
    *   バグ修正や、文字数カウントの精度向上などの細かい調整を行う。

### 6. 将来的な拡張機能（v1.0以降のアイデア）

*   カレンダー形式のヒートマップ表示
*   目標設定機能（月間目標など）と達成率の表示
*   自己ベスト（執筆量TOP3など）のランキング表示
*   複数タグやフォルダ指定への対応

---

この仕様なら、機能がシンプルにまとまっているので、最初のバージョンとして完成させやすいはずです。
開発、応援しています！
